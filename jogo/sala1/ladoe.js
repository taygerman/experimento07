let trem = img = [/*lista onde as imagem vão ficar*/];
let tempo = function (a=trem) {
    let ponto = 1
    if(ponto > 2) {
        /*loop sempre até que vez >=2*/
            //dentro tem um if que verifica atraves de cor se a ponta do trem passou pela linha preta e retorna o valor do cronometro se na vez 1 amarzena a varivel temp1 se não no tem2
        return temp2-temp1

 }
};

export tempo;